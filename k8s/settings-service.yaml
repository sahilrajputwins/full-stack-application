apiVersion: v1
kind: Service
metadata:
  name: settings-service
  namespace: microservices
spec:
  type: NodePort
  selector:
    app: settings-service
  ports:
    - port: 3003
      targetPort: 3003
      nodePort: 30033